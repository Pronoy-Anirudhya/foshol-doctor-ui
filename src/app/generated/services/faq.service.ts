/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext } from '@angular/common/http';
import { Injectable } from '@angular/core';
import { firstValueFrom } from 'rxjs';

import { BaseService } from '../base-service';
import { ApiConfiguration } from '../api-configuration';
import { StrictHttpResponse } from '../strict-http-response';

import { voiceSearchFaq } from '../fn/faq/voice-search-faq';
import { VoiceSearchFaq$Params } from '../fn/faq/voice-search-faq';
import { VoiceSearchResult } from '../models/voice-search-result';

@Injectable({ providedIn: 'root' })
export class FaqService extends BaseService {
  constructor(config: ApiConfiguration, http: HttpClient) {
    super(config, http);
  }

  /** Path part for operation `voiceSearchFaq()` */
  static readonly VoiceSearchFaqPath = '/api/v1/faq/voice-search';

  /**
   * Farmer voice lookup of registered KDB diseases (role FARMER).
   *
   * Transcribes speech and returns ranked catalogue disease candidates only. Does not create a diagnosis case, does not publish an advisory, and does not return remedy text. After the farmer confirms a diseaseId, load remedies from GET /api/v1/diseases/{diseaseId}/remedies.
   *
   * This method provides access to the full `HttpResponse`, allowing access to response headers.
   * To access only the response body, use `voiceSearchFaq()` instead.
   *
   * This method sends `multipart/form-data` and handles request body of type `multipart/form-data`.
   */
  voiceSearchFaq$Response(params: VoiceSearchFaq$Params, context?: HttpContext): Promise<StrictHttpResponse<VoiceSearchResult>> {
    const obs = voiceSearchFaq(this.http, this.rootUrl, params, context);
    return firstValueFrom(obs);
  }

  /**
   * Farmer voice lookup of registered KDB diseases (role FARMER).
   *
   * Transcribes speech and returns ranked catalogue disease candidates only. Does not create a diagnosis case, does not publish an advisory, and does not return remedy text. After the farmer confirms a diseaseId, load remedies from GET /api/v1/diseases/{diseaseId}/remedies.
   *
   * This method provides access only to the response body.
   * To access the full response (for headers, for example), `voiceSearchFaq$Response()` instead.
   *
   * This method sends `multipart/form-data` and handles request body of type `multipart/form-data`.
   */
  voiceSearchFaq(params: VoiceSearchFaq$Params, context?: HttpContext): Promise<VoiceSearchResult> {
    const resp = this.voiceSearchFaq$Response(params, context);
    return resp.then((r: StrictHttpResponse<VoiceSearchResult>): VoiceSearchResult => r.body);
  }

}
