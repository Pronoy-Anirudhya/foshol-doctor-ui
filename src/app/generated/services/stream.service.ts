/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext } from '@angular/common/http';
import { Injectable } from '@angular/core';
import { firstValueFrom } from 'rxjs';

import { BaseService } from '../base-service';
import { ApiConfiguration } from '../api-configuration';
import { StrictHttpResponse } from '../strict-http-response';

import { openEventStream } from '../fn/stream/open-event-stream';
import { OpenEventStream$Params } from '../fn/stream/open-event-stream';

@Injectable({ providedIn: 'root' })
export class StreamService extends BaseService {
  constructor(config: ApiConfiguration, http: HttpClient) {
    super(config, http);
  }

  /** Path part for operation `openEventStream()` */
  static readonly OpenEventStreamPath = '/api/v1/stream';

  /**
   * Server-sent events for the authenticated principal.
   *
   * Farmers receive events for their own cases only; officers receive queue events and assigned-officer KPI warnings. Event names on the wire are `case-status`, `advisory`, `queue`, `kpi`, `resync` and `reconnect`.
   *
   * This method provides access to the full `HttpResponse`, allowing access to response headers.
   * To access only the response body, use `openEventStream()` instead.
   *
   * This method doesn't expect any request body.
   */
  openEventStream$Response(params?: OpenEventStream$Params, context?: HttpContext): Promise<StrictHttpResponse<string>> {
    const obs = openEventStream(this.http, this.rootUrl, params, context);
    return firstValueFrom(obs);
  }

  /**
   * Server-sent events for the authenticated principal.
   *
   * Farmers receive events for their own cases only; officers receive queue events and assigned-officer KPI warnings. Event names on the wire are `case-status`, `advisory`, `queue`, `kpi`, `resync` and `reconnect`.
   *
   * This method provides access only to the response body.
   * To access the full response (for headers, for example), `openEventStream$Response()` instead.
   *
   * This method doesn't expect any request body.
   */
  openEventStream(params?: OpenEventStream$Params, context?: HttpContext): Promise<string> {
    const resp = this.openEventStream$Response(params, context);
    return resp.then((r: StrictHttpResponse<string>): string => r.body);
  }

}
