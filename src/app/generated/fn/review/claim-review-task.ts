/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ReviewTask } from '../../models/review-task';

export interface ClaimReviewTask$Params {
  taskId: string;
}

export function claimReviewTask(http: HttpClient, rootUrl: string, params: ClaimReviewTask$Params, context?: HttpContext): Observable<StrictHttpResponse<ReviewTask>> {
  const rb = new RequestBuilder(rootUrl, claimReviewTask.PATH, 'post');
  if (params) {
    rb.path('taskId', params.taskId, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ReviewTask>;
    })
  );
}

claimReviewTask.PATH = '/api/v1/review/tasks/{taskId}/claim';
