/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ColleagueOfficer } from '../../models/colleague-officer';

export interface ListDistrictOfficers$Params {
}

export function listDistrictOfficers(http: HttpClient, rootUrl: string, params?: ListDistrictOfficers$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<ColleagueOfficer>>> {
  const rb = new RequestBuilder(rootUrl, listDistrictOfficers.PATH, 'get');
  if (params) {
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<ColleagueOfficer>>;
    })
  );
}

listDistrictOfficers.PATH = '/api/v1/review/officers';
