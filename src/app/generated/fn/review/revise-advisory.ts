/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { Advisory } from '../../models/advisory';
import { PublishAdvisoryRequest } from '../../models/publish-advisory-request';

export interface ReviseAdvisory$Params {
  advisoryId: string;
      body: PublishAdvisoryRequest
}

export function reviseAdvisory(http: HttpClient, rootUrl: string, params: ReviseAdvisory$Params, context?: HttpContext): Observable<StrictHttpResponse<Advisory>> {
  const rb = new RequestBuilder(rootUrl, reviseAdvisory.PATH, 'post');
  if (params) {
    rb.path('advisoryId', params.advisoryId, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Advisory>;
    })
  );
}

reviseAdvisory.PATH = '/api/v1/advisories/{advisoryId}/revise';
