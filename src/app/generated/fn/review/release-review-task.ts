/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ReviewTask } from '../../models/review-task';

export interface ReleaseReviewTask$Params {
  taskId: string;
}

export function releaseReviewTask(http: HttpClient, rootUrl: string, params: ReleaseReviewTask$Params, context?: HttpContext): Observable<StrictHttpResponse<ReviewTask>> {
  const rb = new RequestBuilder(rootUrl, releaseReviewTask.PATH, 'post');
  if (params) {
    rb.path('taskId', params.taskId, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ReviewTask>;
    })
  );
}

releaseReviewTask.PATH = '/api/v1/review/tasks/{taskId}/release';
