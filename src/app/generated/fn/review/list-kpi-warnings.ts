/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { KpiWarning } from '../../models/kpi-warning';

export interface ListKpiWarnings$Params {
}

export function listKpiWarnings(http: HttpClient, rootUrl: string, params?: ListKpiWarnings$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<KpiWarning>>> {
  const rb = new RequestBuilder(rootUrl, listKpiWarnings.PATH, 'get');
  if (params) {
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<KpiWarning>>;
    })
  );
}

listKpiWarnings.PATH = '/api/v1/review/kpi-warnings';
