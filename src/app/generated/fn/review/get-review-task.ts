/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ReviewCaseDetail } from '../../models/review-case-detail';

export interface GetReviewTask$Params {
  taskId: string;
}

export function getReviewTask(http: HttpClient, rootUrl: string, params: GetReviewTask$Params, context?: HttpContext): Observable<StrictHttpResponse<ReviewCaseDetail>> {
  const rb = new RequestBuilder(rootUrl, getReviewTask.PATH, 'get');
  if (params) {
    rb.path('taskId', params.taskId, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ReviewCaseDetail>;
    })
  );
}

getReviewTask.PATH = '/api/v1/review/tasks/{taskId}';
