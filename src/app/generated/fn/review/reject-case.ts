/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { RejectCaseRequest } from '../../models/reject-case-request';
import { Rejection } from '../../models/rejection';

export interface RejectCase$Params {
  taskId: string;
      body: RejectCaseRequest
}

export function rejectCase(http: HttpClient, rootUrl: string, params: RejectCase$Params, context?: HttpContext): Observable<StrictHttpResponse<Rejection>> {
  const rb = new RequestBuilder(rootUrl, rejectCase.PATH, 'post');
  if (params) {
    rb.path('taskId', params.taskId, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Rejection>;
    })
  );
}

rejectCase.PATH = '/api/v1/review/tasks/{taskId}/reject';
