/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { BulkApproveRequest } from '../../models/bulk-approve-request';
import { BulkOperationResult } from '../../models/bulk-operation-result';

export interface BulkApproveReviewTasks$Params {
      body: BulkApproveRequest
}

export function bulkApproveReviewTasks(http: HttpClient, rootUrl: string, params: BulkApproveReviewTasks$Params, context?: HttpContext): Observable<StrictHttpResponse<BulkOperationResult>> {
  const rb = new RequestBuilder(rootUrl, bulkApproveReviewTasks.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<BulkOperationResult>;
    })
  );
}

bulkApproveReviewTasks.PATH = '/api/v1/review/tasks/bulk-approve';
