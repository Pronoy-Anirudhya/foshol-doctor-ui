/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { PageOfOfficerQueueRow } from '../../models/page-of-officer-queue-row';

export interface GetReviewQueue$Params {
  state?: 'PENDING' | 'CLAIMED' | 'DONE' | 'REJECTED';
  page?: number;
  size?: number;
}

export function getReviewQueue(http: HttpClient, rootUrl: string, params?: GetReviewQueue$Params, context?: HttpContext): Observable<StrictHttpResponse<PageOfOfficerQueueRow>> {
  const rb = new RequestBuilder(rootUrl, getReviewQueue.PATH, 'get');
  if (params) {
    rb.query('state', params.state, {});
    rb.query('page', params.page, {});
    rb.query('size', params.size, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PageOfOfficerQueueRow>;
    })
  );
}

getReviewQueue.PATH = '/api/v1/review/queue';
