/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { Advisory } from '../../models/advisory';

export interface GetCaseAdvisory$Params {
  caseId: string;
}

export function getCaseAdvisory(http: HttpClient, rootUrl: string, params: GetCaseAdvisory$Params, context?: HttpContext): Observable<StrictHttpResponse<Advisory>> {
  const rb = new RequestBuilder(rootUrl, getCaseAdvisory.PATH, 'get');
  if (params) {
    rb.path('caseId', params.caseId, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Advisory>;
    })
  );
}

getCaseAdvisory.PATH = '/api/v1/cases/{caseId}/advisory';
