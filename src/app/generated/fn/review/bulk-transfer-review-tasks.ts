/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { BulkOperationResult } from '../../models/bulk-operation-result';
import { BulkTransferRequest } from '../../models/bulk-transfer-request';

export interface BulkTransferReviewTasks$Params {
      body: BulkTransferRequest
}

export function bulkTransferReviewTasks(http: HttpClient, rootUrl: string, params: BulkTransferReviewTasks$Params, context?: HttpContext): Observable<StrictHttpResponse<BulkOperationResult>> {
  const rb = new RequestBuilder(rootUrl, bulkTransferReviewTasks.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<BulkOperationResult>;
    })
  );
}

bulkTransferReviewTasks.PATH = '/api/v1/review/tasks/bulk-transfer';
