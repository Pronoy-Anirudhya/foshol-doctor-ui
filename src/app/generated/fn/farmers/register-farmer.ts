/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { FarmerRecord } from '../../models/farmer-record';
import { RegisterFarmerRequest } from '../../models/register-farmer-request';

export interface RegisterFarmer$Params {
  'Idempotency-Key': string;
      body: RegisterFarmerRequest
}

export function registerFarmer(http: HttpClient, rootUrl: string, params: RegisterFarmer$Params, context?: HttpContext): Observable<StrictHttpResponse<FarmerRecord>> {
  const rb = new RequestBuilder(rootUrl, registerFarmer.PATH, 'post');
  if (params) {
    rb.header('Idempotency-Key', params['Idempotency-Key'], {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<FarmerRecord>;
    })
  );
}

registerFarmer.PATH = '/api/v1/farmers';
