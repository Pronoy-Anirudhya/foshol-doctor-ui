/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { FarmerImportResult } from '../../models/farmer-import-result';

export interface ImportFarmers$Params {
      body: {

/**
 * UTF-8 CSV matching the template header, max foshol.identity.bulk.max-bytes
 */
'file': Blob;
}
}

export function importFarmers(http: HttpClient, rootUrl: string, params: ImportFarmers$Params, context?: HttpContext): Observable<StrictHttpResponse<FarmerImportResult>> {
  const rb = new RequestBuilder(rootUrl, importFarmers.PATH, 'post');
  if (params) {
    rb.body(params.body, 'multipart/form-data');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<FarmerImportResult>;
    })
  );
}

importFarmers.PATH = '/api/v1/farmers/import';
