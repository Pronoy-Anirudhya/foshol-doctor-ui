/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { PageOfFarmerRecord } from '../../models/page-of-farmer-record';

export interface ListFarmers$Params {
  page?: number;
  size?: number;

/**
 * Case-insensitive name contains. Must not be combined with phone.
 */
  q?: string;

/**
 * Exact phone lookup after E.164 normalisation. Never echoed. Must not be combined with q.
 */
  phone?: string;
}

export function listFarmers(http: HttpClient, rootUrl: string, params?: ListFarmers$Params, context?: HttpContext): Observable<StrictHttpResponse<PageOfFarmerRecord>> {
  const rb = new RequestBuilder(rootUrl, listFarmers.PATH, 'get');
  if (params) {
    rb.query('page', params.page, {});
    rb.query('size', params.size, {});
    rb.query('q', params.q, {});
    rb.query('phone', params.phone, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PageOfFarmerRecord>;
    })
  );
}

listFarmers.PATH = '/api/v1/farmers';
