/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { FarmerRecord } from '../../models/farmer-record';

export interface GetFarmer$Params {
  farmerId: string;
}

export function getFarmer(http: HttpClient, rootUrl: string, params: GetFarmer$Params, context?: HttpContext): Observable<StrictHttpResponse<FarmerRecord>> {
  const rb = new RequestBuilder(rootUrl, getFarmer.PATH, 'get');
  if (params) {
    rb.path('farmerId', params.farmerId, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<FarmerRecord>;
    })
  );
}

getFarmer.PATH = '/api/v1/farmers/{farmerId}';
