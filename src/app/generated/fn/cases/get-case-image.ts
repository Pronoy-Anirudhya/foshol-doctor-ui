/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';


export interface GetCaseImage$Params {
  caseId: string;
  imageId: string;
  variant?: 'ORIGINAL' | 'DERIVATIVE';
}

export function getCaseImage(http: HttpClient, rootUrl: string, params: GetCaseImage$Params, context?: HttpContext): Observable<StrictHttpResponse<void>> {
  const rb = new RequestBuilder(rootUrl, getCaseImage.PATH, 'get');
  if (params) {
    rb.path('caseId', params.caseId, {});
    rb.path('imageId', params.imageId, {});
    rb.query('variant', params.variant, {});
  }

  return http.request(
    rb.build({ responseType: 'text', accept: '*/*', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return (r as HttpResponse<any>).clone({ body: undefined }) as StrictHttpResponse<void>;
    })
  );
}

getCaseImage.PATH = '/api/v1/cases/{caseId}/images/{imageId}/content';
