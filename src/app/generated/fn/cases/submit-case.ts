/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CaseAccepted } from '../../models/case-accepted';

export interface SubmitCase$Params {
  'Idempotency-Key': string;
      body: {
'cropId': string;
'noteBn'?: string;

/**
 * Set when resubmitting after a rejection
 */
'parentCaseId'?: string;
'images': Array<Blob>;
'audio'?: Blob;
}
}

export function submitCase(http: HttpClient, rootUrl: string, params: SubmitCase$Params, context?: HttpContext): Observable<StrictHttpResponse<CaseAccepted>> {
  const rb = new RequestBuilder(rootUrl, submitCase.PATH, 'post');
  if (params) {
    rb.header('Idempotency-Key', params['Idempotency-Key'], {});
    rb.body(params.body, 'multipart/form-data');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<CaseAccepted>;
    })
  );
}

submitCase.PATH = '/api/v1/cases';
