/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { AnalysisDetail } from '../../models/analysis-detail';

export interface GetCaseAnalysis$Params {
  caseId: string;
}

export function getCaseAnalysis(http: HttpClient, rootUrl: string, params: GetCaseAnalysis$Params, context?: HttpContext): Observable<StrictHttpResponse<AnalysisDetail>> {
  const rb = new RequestBuilder(rootUrl, getCaseAnalysis.PATH, 'get');
  if (params) {
    rb.path('caseId', params.caseId, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<AnalysisDetail>;
    })
  );
}

getCaseAnalysis.PATH = '/api/v1/cases/{caseId}/analysis';
