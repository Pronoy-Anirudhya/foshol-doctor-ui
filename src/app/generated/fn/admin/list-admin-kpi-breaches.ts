/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { PageOfKpiBreach } from '../../models/page-of-kpi-breach';

export interface ListAdminKpiBreaches$Params {
  kind?: 'ASSIGNMENT' | 'RESOLUTION';
  officerId?: string;
  page?: number;
  size?: number;
}

export function listAdminKpiBreaches(http: HttpClient, rootUrl: string, params?: ListAdminKpiBreaches$Params, context?: HttpContext): Observable<StrictHttpResponse<PageOfKpiBreach>> {
  const rb = new RequestBuilder(rootUrl, listAdminKpiBreaches.PATH, 'get');
  if (params) {
    rb.query('kind', params.kind, {});
    rb.query('officerId', params.officerId, {});
    rb.query('page', params.page, {});
    rb.query('size', params.size, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PageOfKpiBreach>;
    })
  );
}

listAdminKpiBreaches.PATH = '/api/v1/admin/kpis/breaches';
