/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { PageOfOfficerQueueRow } from '../../models/page-of-officer-queue-row';

export interface ListAdminCases$Params {
  period?: 'TODAY' | 'MONTH' | 'YEAR' | 'LIFETIME';
  state?: 'PENDING' | 'CLAIMED' | 'DONE' | 'REJECTED' | 'ALL';
  kpi?: 'ASSIGNMENT' | 'RESOLUTION';
  officerId?: string;
  cropCode?: string;
  decisionPath?: 'PRIMARY' | 'SECONDARY' | 'UNDETERMINED';
  resubmission?: boolean;
  page?: number;
  size?: number;
}

export function listAdminCases(http: HttpClient, rootUrl: string, params?: ListAdminCases$Params, context?: HttpContext): Observable<StrictHttpResponse<PageOfOfficerQueueRow>> {
  const rb = new RequestBuilder(rootUrl, listAdminCases.PATH, 'get');
  if (params) {
    rb.query('period', params.period, {});
    rb.query('state', params.state, {});
    rb.query('kpi', params.kpi, {});
    rb.query('officerId', params.officerId, {});
    rb.query('cropCode', params.cropCode, {});
    rb.query('decisionPath', params.decisionPath, {});
    rb.query('resubmission', params.resubmission, {});
    rb.query('page', params.page, {});
    rb.query('size', params.size, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<PageOfOfficerQueueRow>;
    })
  );
}

listAdminCases.PATH = '/api/v1/admin/cases';
