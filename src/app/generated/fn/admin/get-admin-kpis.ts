/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { AdminKpiSummary } from '../../models/admin-kpi-summary';

export interface GetAdminKpis$Params {
}

export function getAdminKpis(http: HttpClient, rootUrl: string, params?: GetAdminKpis$Params, context?: HttpContext): Observable<StrictHttpResponse<AdminKpiSummary>> {
  const rb = new RequestBuilder(rootUrl, getAdminKpis.PATH, 'get');
  if (params) {
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<AdminKpiSummary>;
    })
  );
}

getAdminKpis.PATH = '/api/v1/admin/kpis';
