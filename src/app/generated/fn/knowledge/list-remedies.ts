/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { Remedy } from '../../models/remedy';

export interface ListRemedies$Params {
  diseaseId: string;
}

export function listRemedies(http: HttpClient, rootUrl: string, params: ListRemedies$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<Remedy>>> {
  const rb = new RequestBuilder(rootUrl, listRemedies.PATH, 'get');
  if (params) {
    rb.path('diseaseId', params.diseaseId, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<Remedy>>;
    })
  );
}

listRemedies.PATH = '/api/v1/diseases/{diseaseId}/remedies';
