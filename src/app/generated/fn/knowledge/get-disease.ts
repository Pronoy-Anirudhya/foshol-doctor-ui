/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { Disease } from '../../models/disease';

export interface GetDisease$Params {
  diseaseId: string;
}

export function getDisease(http: HttpClient, rootUrl: string, params: GetDisease$Params, context?: HttpContext): Observable<StrictHttpResponse<Disease>> {
  const rb = new RequestBuilder(rootUrl, getDisease.PATH, 'get');
  if (params) {
    rb.path('diseaseId', params.diseaseId, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Disease>;
    })
  );
}

getDisease.PATH = '/api/v1/diseases/{diseaseId}';
