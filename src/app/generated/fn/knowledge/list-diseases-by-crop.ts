/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { Disease } from '../../models/disease';

export interface ListDiseasesByCrop$Params {
  cropId: string;
}

export function listDiseasesByCrop(http: HttpClient, rootUrl: string, params: ListDiseasesByCrop$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<Disease>>> {
  const rb = new RequestBuilder(rootUrl, listDiseasesByCrop.PATH, 'get');
  if (params) {
    rb.path('cropId', params.cropId, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<Disease>>;
    })
  );
}

listDiseasesByCrop.PATH = '/api/v1/crops/{cropId}/diseases';
