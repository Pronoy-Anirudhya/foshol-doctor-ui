# Angular specific
/dist/
/out-tsc/
/tmp/
/coverage/
/e2e/test-output/
/.angular/
.angular/

# Node modules and dependency files
/node_modules/
# package-lock.json is COMMITTED: the Docker build runs `npm ci`, which needs it, and a
# reproducible image is worth more than a quiet diff. (was ignored before containerisation)
/yarn.lock

# Environment files
/.env

# Angular CLI and build artefacts
/.angular-cli.json
/.ng/

# TypeScript cache
*.tsbuildinfo

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# macOS Finder metadata
.DS_Store
